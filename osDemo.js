// OS module // This module provides information about the system
import os from 'os'; 

// Methods in the OS module
// userInfo
console.log(os.userInfo());

// Methods that have to do with memory
// 1. totalmem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem());

// cpus()
console.log(os.cpus());
